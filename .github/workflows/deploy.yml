name: DeployBot
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: CheckoutCode
        uses: actions/checkout@v4
        with:
          clean: false
      - name: StartContainer
        run: sudo docker compose up --build -d